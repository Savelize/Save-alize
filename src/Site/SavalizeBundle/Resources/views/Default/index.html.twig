<!doctype html>
<html>
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Up and Running with Bootstrap</title>
<link href="{{asset('css/bootstrap.css')}}" rel="stylesheet" type="text/css">
<link href="{{asset('css/bootstrap-responsive.css')}}" rel="stylesheet" type="text/css">
<link rel="stylesheet" href="{{asset('font-awesome/css/font-awesome.min.css')}}">
<link rel="stylesheet" href="{{asset('css/main.css')}}">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<!-- HTML5 shim for IE backwards compatibility -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
</head>

<body>
<div class="container-fluid" >
	<div class="navbar" >
  <div class="navbar-inner">
    <a class="brand" href="#">Save and Socialize</a>
    <ul class="nav pull-right">
   <form action="{{ path('user_signIn') }}" method="post" {{ form_enctype(SignInform) }} class="navbar-form form-inline">
        {{ form_widget(SignInform.User_Name) }}
        {{ form_widget(SignInform.Password) }}
           {{ form_rest(SignInform) }}
                            <input type="submit" value="Log in" class="btn">
                      
                    </form>
    </ul>
  </div>
</div>
<br>
	<div class="row-fluid">
    	<header class="span9">
            <p><strong>Save-alize</strong> It is the first free web application for tracking personal finance</p>
    	<ul class="thumbnails">
        	<li class="span4">
            	<article class="thumbnail">
                	<img src="{{asset('img/history.png')}}" alt="History"/>
                    <h3>History</h3>
                    <p>Track your history of purchases per day or week or month.</p>
                </article>
            </li>        
            
            <li class="span4">
            	<article class="thumbnail">
                    <img src="{{asset('img/Charts.jpg')}}" alt="Reports"/>
                    <h3>Reports</h3>
                    <p>Generate reports using filters with different types of charts.</p>
                </article>
            </li> 
            
            <li class="span4">
            	<article class="thumbnail">
                	<img src="{{asset('img/socialization.jpg')}}" alt="Socialization"/>
                    <h3>Socialization</h3>
                    <p>Contact with your favourit companies directly without mediums and know what your friends are buying</p>
                </article>
            </li> 
            
        </ul>
        </header>     
    <div class="span3 form-signup">
       		<h2>Sign Up</h2>
                <form id="SignUpForm" action="{{ path('user_signUp') }}" method="post" {{ form_enctype(signUpform) }} class="form-inline">
    
    <div class="control-group">
    {{ form_label(signUpform.User_Name) }}
    {{ form_widget(signUpform.User_Name) }}
    {{ form_errors(signUpform.User_Name) }}

    {{ form_rest(signUpform) }}
            
  <div id="myModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Licence Agremment</h3>
  </div>
  <div class="modal-body">
      <p>you must agree on these terms:</p>
      <ul>
          <li>the data will be used by the site for the purpose of surveys and studies,presonal data is private ex:(name,phone,mail,etc..) </li>
          <li>you cant use the feature of generating reports unless you complete all required data</li>
      </ul>
    
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
    <button type="submit" class="btn btn-primary">Accept</button>
  </div>
</div>
            </div>
                    <input id="fakeSupmit" data-toggle="modal" value="Sign Up" class="btn btn-success btn-large" href="#myModal">
                      
                    </form>
       </div> 
       </div>   
</div>
<script src="{{asset('js/jquery.js')}}"></script>
<script src="{{asset('js/bootstrap.min.js')}}"></script>
    <script>
     
        function check(input)
        {
            if (input.value != document.getElementById('form_Confirm_Password').value)
            {
                input.setCustomValidity('Password miss match');
            }
            else
            {
                input.setCustomValidity('');
            }
        }
    </script>
</body>
</html>
